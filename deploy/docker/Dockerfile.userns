FROM node:22-slim
WORKDIR /app
COPY . .
RUN corepack enable
RUN corepack prepare pnpm@10.23.0 --activate
RUN pnpm install --prod
USER 10001:10001
ENV OPENSOUL_STATE_DIR=/data
CMD ["node", "dist/cli.js", "gateway"]
